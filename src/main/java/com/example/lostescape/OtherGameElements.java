package com.example.lostescape;

import javafx.scene.Node;
import javafx.stage.Stage;

public interface OtherGameElements {
    void centerStage(Stage stage);
    void setupMouseEvents(Node root, Stage stage);
}
